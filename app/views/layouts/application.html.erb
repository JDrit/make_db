<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/assets/favicon.ico">
            
    <title>Make DB</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="menu">
      <nav class="top-bar" data-topbar data-options="is_hover: false">
        <ul class="title-area">
		  <!-- Title Area -->
          <li class="name">
            <h1><%= link_to (image_tag("/assets/csh.png", :border => 0, :alt => 'CSH Logo', ) + " Make Database"), root_path %></h1>
		  </li>
		  <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>  
		</ul>
        <section class="top-bar-section">
		  <!-- Left Nav Section -->
		  <ul class="left">
		  </ul>
		  <!-- Right Nav Section -->
          <ul class="right">
            <% if @admin %>  
                <li><%= link_to "Admin Panel", "/admin" %></li>
            <% end %>
            <li><%= link_to "Memebers Home", "https://members.csh.rit.edu" %></li>
            <li><%= link_to "Public Site", "http://csh.rit.edu" %></li>
            <li><%= link_to "Webauth Logout", "https://members.csh.rit.edu/logout" %></li>
		  </ul>
		</section>
	  </nav>
	</div>
    <div id="main" class="row ">
        <div class="large-12 columns" id="container">
            <div class="row panel" id="main-header">
                <h2><%= yield(:title) %></h2>
            </div>
            <div class="row_panel">
                <% flash.each do |key, value| %>
                    <%= content_tag(:div, value, class: "alert-box") %>
                <% end %>
                <%= yield %>
            </div>
        </div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
